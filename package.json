{
  "name": "meu-site-ia",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",               // Comando para desenvolvimento
    "build": "vite build",       // Comando para build
    "preview": "vite preview"    // Comando para visualizar o build
  },
  "dependencies": {
    "vite": "^4.0.0"             // Ferramenta de build e desenvolvimento
  }
}
